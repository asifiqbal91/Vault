# Example configuration for local deployment
# Copy this to terraform.tfvars and fill in your values

docker_host = "unix:///var/run/docker.sock"
# docker_socket_path = "/var/run/docker.sock"  # Optional, defaults to above
# storage_root = "/mnt/workbench/storages/vault"

vault_admin_token = "generate-a-strong-token"
vault_database_url = "postgresql://user:password@postgres:5432/vault"
vault_hostname     = "vault.local"
public_network_name  = "public"
kitchen_network_name = "kitchen"
